# 林赫宮 GitHub Pages Preview

This folder is a static preview for GitHub Pages.

It shows the landing page and form flow only. The full paid workflow needs the Node backend in `../linhe-palace` because report generation calls the local `fortune-report` command and sends email.

Deploy this folder to GitHub Pages to preview the UI.
